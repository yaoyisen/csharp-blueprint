<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CSharpBlueprint.WinUI3.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CSharpBlueprint.WinUI3"
    xmlns:vm="using:CSharpBlueprint.WinUI3.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <XamlUICommand x:Name="CreateEmptySolutionCommand" 
                       ExecuteRequested="CreateEmptySolutionCommand_ExecuteRequested"/>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <MenuBar Grid.Row="0">
            <MenuBarItem Title="开始">
                <MenuFlyoutItem Text="Open..." Command="{StaticResource CreateEmptySolutionCommand}"/>
                <MenuFlyoutItem Text="Save"/>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Exit"/>
            </MenuBarItem>
        </MenuBar>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TreeView Grid.Column="0" ItemsSource="{x:Bind CurrentSolutionViewModel.Items, Mode=OneWay}">
                <TreeView.ItemTemplate>
                    <DataTemplate x:DataType="vm:ITreeViewItem">
                        <TreeViewItem Content="{x:Bind Name}" ItemsSource="{x:Bind Items}"/>
                    </DataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
            <ContentControl Grid.Column="1"/>
        </Grid>
    </Grid>
</Page>
